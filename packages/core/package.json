{
  "name": "@catchvoca/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core logic for CatchVoca (Dexie, SM-2, EventBus)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:ui": "vitest --ui",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix"
  },
  "dependencies": {
    "@catchvoca/types": "workspace:*",
    "dexie": "^3.2.4"
  },
  "devDependencies": {
    "@vitest/ui": "^1.2.0",
    "fake-indexeddb": "^5.0.2",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
